#include "main.h"
#include <stdio.h>

/**
 * main - A simple shell program
 * @argc: int Argument count of the secnd array of strings
 * @argv: Array Array of string arguments to the program
 *
 * Return: int 0 on success or any number above 0 on failure
 */

int main(argc int, argv **char)
{
	return (0);
}
